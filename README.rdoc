== README

This is an application for find restaurants after select the type of food.

First we create our controller and we add several methods to it.

second we create a different rout for each page.

third we created all the views and we linked each other.

forth we created a git repository, connected it to the GitGub in order to have a backup of our progress.

fifth we create an account in Heroku to deploy our application.